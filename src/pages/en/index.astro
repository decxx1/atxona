---
import Layout from '@/layouts/Layout.astro';
import IndexPage from '@/components/pages/Index.astro';

const preloadImages = [
	{
		href:"/images/index/finca.webp",
		as: "image",
		type: "image/webp",
	}
];
import { getI18N } from "@/i18n";

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<Layout
	title={i18n.index.metaData.title}
	canonical="https://fincaatxona.com.ar"
	metaDescription={i18n.index.metaData.description}
	metaImg="https://fincaatxona.com.ar/images/index/finca.webp"
	preLoad={preloadImages}
>
	<IndexPage />
</Layout>
